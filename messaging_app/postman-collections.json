{
  "info": {
    "name": "Messaging App API",
    "description": "Postman collection to test JWT authentication, conversations, and messages.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Auth - Obtain JWT Token",
      "request": {
        "method": "POST",
        "header": [
          { "key": "Content-Type", "value": "application/json" }
        ],
        "url": "{{base_url}}/api/token/",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"username\": \"testuser\",\n  \"password\": \"password123\"\n}"
        }
      }
    },
    {
      "name": "Conversations - Create",
      "request": {
        "method": "POST",
        "header": [
          { "key": "Authorization", "value": "Bearer {{jwt_token}}" },
          { "key": "Content-Type", "value": "application/json" }
        ],
        "url": "{{base_url}}/api/conversations/",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"receiver\": 2\n}"
        }
      }
    },
    {
      "name": "Conversations - Fetch My Conversations",
      "request": {
        "method": "GET",
        "header": [
          { "key": "Authorization", "value": "Bearer {{jwt_token}}" }
        ],
        "url": "{{base_url}}/api/conversations/"
      }
    },
    {
      "name": "Messages - Send Message",
      "request": {
        "method": "POST",
        "header": [
          { "key": "Authorization", "value": "Bearer {{jwt_token}}" },
          { "key": "Content-Type", "value": "application/json" }
        ],
        "url": "{{base_url}}/api/messages/",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"conversation\": 1,\n  \"content\": \"Hello from Postman!\"\n}"
        }
      }
    },
    {
      "name": "Messages - Fetch Messages",
      "request": {
        "method": "GET",
        "header": [
          { "key": "Authorization", "value": "Bearer {{jwt_token}}" }
        ],
        "url": "{{base_url}}/api/messages/?conversation=1"
      }
    },
    {
      "name": "Unauthorized - Try Without Token",
      "request": {
        "method": "GET",
        "url": "{{base_url}}/api/conversations/"
      }
    }
  ],
  "variable": [
    {
      "key": "base_url",
      "value": "http://127.0.0.1:8000"
    },
    {
      "key": "jwt_token",
      "value": ""
    }
  ]
}
